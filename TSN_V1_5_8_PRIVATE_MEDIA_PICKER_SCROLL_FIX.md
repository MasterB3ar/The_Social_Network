# TSN V1.5.8 — Private Media Picker Scroll Fix

Fixed the private-chat picture/GIF picker so it can scroll correctly when many media results are available.

Changes:
- Private chat media picker has a scrollable area.
- Media search panel stays visible while scrolling.
- Mobile height limits prevent the picker from going outside the chat window.
- Updated app.js cache-busting query to v1.5.8.

No new environment variables were added.
